import styled from 'styled-components';

const Container = styled.div`
    position: relative;
    width: 100%;
    max-width: 1200px;
    padding: 0 3em;
    margin: 0 auto;
`;

export default Container;